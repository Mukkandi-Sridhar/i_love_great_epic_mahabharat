{
  "name": "mahabharat",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgGreen.bold\" \"uvicorn backend.main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "install:all": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
